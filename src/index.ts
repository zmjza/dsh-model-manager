/**
 * @module dsh-model-manager
 * Host plugin body — no host-side behavior for the models enhancement
 * plugin. All state rides the existing settings / credentials / llm wire
 * domains; the client half owns the UI.
 */
export function apply(): void {}
